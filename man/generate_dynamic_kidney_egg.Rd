% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/KidneyEgg.R
\name{generate_dynamic_kidney_egg}
\alias{generate_dynamic_kidney_egg}
\title{Generate a Dynamic Kidney–Egg Network (Controlled Window)}
\usage{
generate_dynamic_kidney_egg(
  n,
  m,
  p,
  q,
  T = 10,
  anomaly_start = 5,
  anomaly_end = 7,
  theta = NULL,
  seed = NULL
)
}
\arguments{
\item{n}{Integer. Total number of nodes.}

\item{m}{Integer. Number of Egg nodes during the anomaly window.}

\item{p}{Numeric in \link{0, 1}. Baseline probability for Kidney–Kidney and Kidney–Egg edges.}

\item{q}{Numeric in \link{0, 1}. Egg–Egg probability during anomaly window.}

\item{T}{Integer. Number of time steps (length of the series). Default \code{10}.}

\item{anomaly_start}{Integer. First time step (inclusive) where the Egg is active.}

\item{anomaly_end}{Integer. Last time step (inclusive) where the Egg is active.}

\item{theta}{Optional numeric vector of length \code{n}. Degree correction parameters.}

\item{seed}{Optional integer. Base random seed for reproducibility.}
}
\value{
A list of \eqn{T} adjacency matrices (each \eqn{n \times n}).
}
\description{
Produces a time series of Kidney–Egg graphs. During an “anomaly window”
(\code{anomaly_start} to \code{anomaly_end}), the Egg (size \code{m}) is present
with probabilities \code{p} (for 1–1 and 1–2 edges) and \code{q} (for 2–2 edges).
Outside the window, the network reverts to an Erdős–Rényi-like background
by setting \code{m = 0} and \code{q = p}.
}
\examples{
set.seed(1)
A_list <- generate_dynamic_kidney_egg(
  n = 30, m = 8, p = 0.04, q = 0.2,
  T = 20, anomaly_start = 8, anomaly_end = 14, seed = 99
)
length(A_list)   # 20 graphs

}
\seealso{
\code{\link{generate_kidney_egg}}, \code{\link{generate_sbm}}
}
